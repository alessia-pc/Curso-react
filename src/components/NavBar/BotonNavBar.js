/* import { NavLink } from "react-router-dom";


const BotonNavBar = (iconoBoton = null, nombreBoton = null, linkBoton, listaDesplegable = [] ) => {
  return (
      if(iconoBoton) {
          <NavLink
      to= {linkBoton}
      className={({ isActive }) => (isActive ? "ActiveOption" : "Option")}
    >
      <img src={iconoBoton} height="37px" className="NavBar-icono" alt="logo" />
    </NavLink>
      }
    
  );
  
}

export default BotonNavBar */